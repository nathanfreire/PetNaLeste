09/06/2025
No primeiro dia decidimos iniciar o projeto pelo banco de dados,
onde fizemos a modelagem do banco, criando (Usuario, Animal,
Contato, Endereco e Redes Sociais) no exel, logo após, vinculamos
o banco de dados com o servidor criado no fedora, jogamos os
dados do exel para o workbench e as tabelas foram criadas.

amanhã criar algo em cada tabela, e vincular ao back-and,
testando pelo thunder.

10/06/2025
Revemos alguns conceitos do banco de dados e trocamos algumas configurações do Endereco, após isso começamos as configurações do back-end, onde criamos as pastas, classes, database, interfaces, repositories, services.
começamos a codificação para conectar o banco de dados com o back-end testado pelo thander.

11/06/2025
Continuamos fazendo a conexão do banco de dados com o back-end.
tambem começamos a fazer o front-end.